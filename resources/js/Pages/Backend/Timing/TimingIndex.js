import BackendContainer from "../components/containers/BackendContainer";

export default function TimingIndex({ elements }) {
    return (
        <BackendContainer>
            <h1>timing index</h1>
        </BackendContainer>
    )
}
